# AGENT — Generated code: DO NOT EDIT

> This package is generated by [`@doclient`](https://github.com/Naputt1/doclient) (`doclient.config.ts` + `profile.ts` + `overrides.ts` + `source-lazada.ts`).

**Never edit (overwritten by `pnpm run generate` / `.github/workflows/weekly-generate.yml`):**
- `*.gen.go`, `*.type.gen.go`, `errors.gen.go`, `*_test.go` (generated), `fixtures/*.json`, `.cache/`
- Scaffolds `golazada.go` + `auth.go` — rendered from `templates/client.go` + `templates/auth.go`

**Edit instead:**
- Client logic / signing / `execute()` → `templates/client.go`
- Types, enums, field overrides → `profile.ts` / `overrides.ts` / `doclient.config.ts:mappings`
- Missing Lazada params (e.g. `order_ids`) → `profile.ts` synthetic `IRParam` + `source-lazada.ts`

**After template/profile change:** `pnpm install --frozen-lockfile && pnpm run generate` then `go vet ./... && go test ./...` must pass. Commit template **and** generated output together — weekly `git add -A` will revert a hotfix that touches only `golazada.go`.
